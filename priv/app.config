[

%%
%%
{ambitz, [
   %%
   %% list of cluster nodes
   {seed,       ['a@127.0.0.1']}
   % {seed,       ['thing1@ec2-54-246-18-172.eu-west-1.compute.amazonaws.com']}
]},

{memcache, [
   %%
   %% configuration of ring 
   {ring, [
      {type, ring}            %% ring management algorithms - chord | ring
     ,{m,      64}            %% ring modulo
     ,{q,    4096}            %% number of shards
     ,{n,       2}            %% number of replica
     ,{hash,  sha}            %% hashing algorithm
   ]}
]},

{thingz, [
   %%
   %% configuration of ring 
   {ring, [
      {type, ring}            %% ring management algorithms - chord | ring
     ,{m,      64}            %% ring modulo
     ,{q,    4096}            %% number of shards
     ,{n,       3}            %% number of replica
     ,{hash,  sha}            %% hashing algorithm
   ]}
]},

%%
%%
{permit, [
   %%
   %% token encryption key, use openssl
   %% openssl rand -base64 16 / 24 / 32
   {key,    "FMoiy/fagdN14OGERkzsLEE0qrL/d2QNos/LB8Ke7TI="}
]}


].